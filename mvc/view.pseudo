/** CHANGE: imported view info directly, moved getCurrentSeason to controller and wrapped view in render()**/
function render($username, $h1style)
{
    $title = "<title>Welcome $username</title>"; 
    //display
    echo "<html>
                <head>
                    $title
                </head>
                <body>
                    $h1Style
                        Welcome $username
                    </h1>
                </body>
            </html>";
}
