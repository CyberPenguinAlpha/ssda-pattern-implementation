/** CHANGE: Direct import of model responsability**/

// hash password
$pwHash = hash($pw1);

// initialize database
$db = getDatabase('myHost', 'mypassword', 'myDb');
$table = "users";

// create user
$success = $db->query("INSERT INTO $table (username, password) 
    VALUES ($username, $pwHash);");
if(!$success)
    return redirectBack(500);
